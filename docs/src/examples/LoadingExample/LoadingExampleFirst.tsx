import * as React from 'react';
import { Loading } from '../../../../src/components/';
import * as styles from '../../styles/components-page.scss';

const LoadingExampleFirst = () => (
  <div className={styles.example}>
    <Loading/>
  </div>
);

export default LoadingExampleFirst;
