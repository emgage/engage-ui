import { IDocument } from '../../Types';

const PositionedOverlayState: IDocument = {
  id: 'positionedoverlay',
  heading: 'Positioned Overlay Component',
  subheading: `Positioned Overlay is a helper component. It is used in Tooltip and PopOver components.`,
};

export default PositionedOverlayState;
